----------------------------------------------------------------------------------------------------

+ main() {
    x = 2
    if (x > 1) {
        -- "yes"
    } else {
        -- "no"
    }
    -- "done"
}

- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

yes
done

----------------------------------------------------------------------------------------------------

+ check(x:Int32) {
    if (x > 10) {
        -- "a"
    } else if (x >= 2) {
        -- "b"
    } else {
        -- "c"
    }
    -- "done"
}
    
+ main() {
    check(1)
    check(11)
    check(3)
}

- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

c
done
a
done
b
done

----------------------------------------------------------------------------------------------------

+ main() {
    x = 1
    -- if x > 10 -> 1

    x = 11
    -- if x > 10 -> 1

    x = 1
    -- if x > 10 -> 1 else 3

    x = 11
    -- if x > 10 -> 1 else 3

    x = 1
    -- if x > 10 -> 1 or x > 5 -> 2

    x = 6
    -- if x > 10 -> 1 or x > 5 -> 2

    x = 11
    -- if x > 10 -> 1 or x > 5 -> 2 else 3

    x = 6
    -- if x > 10 -> 1 or x > 5 -> 2 else 3

    x = 1
    -- if x > 10 -> 1 or x > 5 -> 2 else 3
}

- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

0
1
3
1
0
2
1
2
3

----------------------------------------------------------------------------------------------------
